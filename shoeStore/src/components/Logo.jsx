import React from "react";
import logo from "../assets/logo.png";

const Logo = () => {
  return (
    <div className="logo">
      <img src={logo} alt="logo" width={`90px`} />
    </div>
  );
};

export default Logo;
